<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.orderservice.mapper.OrderMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.orderservice.model.Order">
        <id column="id" property="id" />
        <result column="status" property="status" />
        <result column="customer_id" property="customerId" />
        <result column="shipment_id" property="shipmentId" />
        <result column="total_price" property="totalPrice" />
        <result column="messages" property="messages" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, status, customer_id, shipment_id, total_price, messages
    </sql>

</mapper>
